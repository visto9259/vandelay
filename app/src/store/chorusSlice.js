import {createSlice} from "@reduxjs/toolkit";

const chorusSlice = createSlice({
  name: "chorus",
  initialState: {
    loading: true,
  },
  reducers: {

  }
});
export const {} = chorusSlice.actions;
export default chorusSlice.reducer;